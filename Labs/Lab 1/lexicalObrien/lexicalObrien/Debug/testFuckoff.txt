#include stdio.h
int main(){
	void Fibonacci( int n );
	int x = 10;
	Fibonacci( n );
}

void Fibonacci( int n ){
	fibArray[]; 
	cout << The first << n << “ terms of Fibonacci sequence are: ” << endl;
	for( int i = 1; i < n; i++ ){
		cout << “Fibonacci ” << i << “ is: ” << fibArray[i] << endl;
	}
}
"end in a string"


/*buffer.at(index) == '==' ||
			buffer.at(index) == '!=' ||
			buffer.at(index) == '<=' ||
			buffer.at(index) == '>=' ||
			buffer.at(index) == '<<' ||
			buffer.at(index) == '>>' ||
			buffer.at(index) == '||'*/